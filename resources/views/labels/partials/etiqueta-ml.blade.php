{{-- NOV-208 v2 — etiqueta ML replica posicional: HTML gerado pelo MlLabelPdf
     a partir do content stream do PDF (layout identico ao original, nitido,
     sem a faixa "Recorte essa parte"). --}}
<div class="eml-wrap">{!! $ml !!}</div>
